# 🚀 دليل النشر على Vercel - WejdanAI

دليل خطوة بخطوة للنشر من iPhone/iPad أو أي جهاز.

---

## 📱 النشر من iPhone/iPad (موصى به)

### الخطوة 1: إنشاء حساب Vercel

1. افتح Safari على iPhone
2. اذهب إلى: https://vercel.com/signup
3. اختر "Continue with GitHub"
4. سجّل الدخول بحساب GitHub الخاص بك
5. امنح Vercel الأذونات المطلوبة

---

### الخطوة 2: استيراد المشروع

1. من لوحة تحكم Vercel، اضغط **"Add New..."**
2. اختر **"Project"**
3. ستظهر قائمة مشاريعك من GitHub
4. ابحث عن **"WejdanAI"**
5. اضغط **"Import"** بجانب المشروع

---

### الخطوة 3: تكوين المشروع

#### 3.1 - اختيار الفرع (Branch)

في صفحة التكوين:
- **Framework Preset:** سيكتشف Nuxt.js تلقائياً ✅
- **Root Directory:** اتركه كما هو `./`
- **Build Command:** `pnpm build` (تلقائي)
- **Output Directory:** `.output/public` (تلقائي)

#### 3.2 - اختيار الفرع الصحيح

⚠️ **مهم جداً:**
- اضغط على **"Git Branch"**
- اختر: `claude/ai-cicd-automation-xV0YP`
- (هذا الفرع يحتوي على واجهة الدردشة الكاملة)

---

### الخطوة 4: إضافة المتغيرات البيئية (Environment Variables)

هذه هي الخطوة الأهم! اضغط على **"Environment Variables"**.

#### 4.1 - قاعدة البيانات (إلزامي)

**الخيار A: استخدام Vercel Postgres (موصى به)**

1. في نفس صفحة التكوين، اضغط **"Storage"**
2. اختر **"Create Postgres Database"**
3. اتبع التعليمات
4. سيتم إضافة `POSTGRES_URL` تلقائياً ✅

**الخيار B: استخدام قاعدة بيانات خارجية**

إذا كان لديك Postgres خارجي:

```
Name:  POSTGRES_URL
Value: postgresql://username:password@host:5432/database
```

#### 4.2 - مزودي الذكاء الاصطناعي (واحد على الأقل)

**OpenAI (موصى به للجودة)**

احصل على API Key:
1. اذهب إلى: https://platform.openai.com/api-keys
2. سجّل الدخول أو أنشئ حساب
3. اضغط **"Create new secret key"**
4. انسخ الـ key (يبدأ بـ `sk-proj-...`)

في Vercel:
```
Name:  OPENAI_API_KEY
Value: sk-proj-xxxxxxxxxxxxxxxxx
```

**DeepSeek (موصى به للسعر)**

احصل على API Key:
1. اذهب إلى: https://platform.deepseek.com/api_keys
2. سجّل الدخول
3. أنشئ API key جديد
4. انسخه

في Vercel:
```
Name:  DEEPSEEK_API_KEY
Value: sk-xxxxxxxxxxxxxxxxx
```

**Google Gemini (مجاني مع حد)**

احصل على API Key:
1. اذهب إلى: https://makersuite.google.com/app/apikey
2. سجّل الدخول بحساب Google
3. اضغط **"Create API key"**
4. انسخه

في Vercel:
```
Name:  GEMINI_API_KEY
Value: AIzaSyxxxxxxxxxxxxxxxxx
```

#### 4.3 - كيفية إضافة المتغيرات

لكل متغير:
1. اضغط **"Add Another"** (أو استخدم الحقل الأول)
2. أدخل **Name** (مثل `OPENAI_API_KEY`)
3. أدخل **Value** (الـ key الفعلي)
4. اترك Environment على **"Production, Preview, Development"** ✅
5. كرر لكل متغير

---

### الخطوة 5: النشر! 🚀

1. تأكد من إضافة `POSTGRES_URL` و **واحد على الأقل** من AI keys
2. اضغط **"Deploy"** في الأسفل
3. انتظر 2-5 دقائق (شاهد البناء يتم)

---

### الخطوة 6: الوصول إلى التطبيق

عند اكتمال النشر:

1. ستظهر رسالة "Congratulations!" 🎉
2. ستجد رابط التطبيق مثل: `https://wejdan-ai-xxx.vercel.app`
3. اضغط **"Visit"** أو انسخ الرابط
4. أضف `/chat` في نهاية الرابط:
   ```
   https://wejdan-ai-xxx.vercel.app/chat
   ```

---

## ✅ قائمة التحقق

- [ ] حساب Vercel جاهز
- [ ] المشروع مستورد من GitHub
- [ ] الفرع الصحيح: `claude/ai-cicd-automation-xV0YP`
- [ ] `POSTGRES_URL` مُضاف
- [ ] واحد على الأقل من AI keys مُضاف:
  - [ ] `OPENAI_API_KEY` أو
  - [ ] `DEEPSEEK_API_KEY` أو
  - [ ] `GEMINI_API_KEY`
- [ ] النشر مكتمل بنجاح ✅
- [ ] التطبيق يعمل على `/chat` ✅

---

## 🐛 حل المشاكل

### مشكلة: "Build Failed"

**السبب المحتمل:** نقص المتغيرات البيئية

**الحل:**
1. اذهب إلى: Settings → Environment Variables
2. تأكد من إضافة `POSTGRES_URL`
3. أضف واحد على الأقل من AI keys
4. اذهب إلى Deployments
5. اضغط على آخر deployment فاشل
6. اضغط **"Redeploy"**

---

### مشكلة: "No provider available"

**السبب:** لم تُضف أي AI key

**الحل:**
1. Settings → Environment Variables
2. أضف واحد من:
   - `OPENAI_API_KEY=sk-...`
   - `DEEPSEEK_API_KEY=sk-...`
   - `GEMINI_API_KEY=...`
3. Redeploy

---

### مشكلة: "Database connection failed"

**السبب:** `POSTGRES_URL` خاطئ أو مفقود

**الحل:**

**إذا استخدمت Vercel Postgres:**
1. Storage → Postgres
2. تأكد من أن الـ database "Connected"
3. انسخ Connection String
4. Settings → Environment Variables
5. تحديث `POSTGRES_URL`

**إذا استخدمت Postgres خارجي:**
1. تحقق من صحة الـ URL:
   ```
   postgresql://username:password@host:5432/dbname
   ```
2. تأكد من السماح بالاتصال من Vercel IPs

---

### مشكلة: الصفحة تظهر 404

**السبب:** تحاول فتح `/` بدلاً من `/chat`

**الحل:**
افتح: `https://your-app.vercel.app/chat` (أضف `/chat`)

---

## 🔧 إعدادات متقدمة (اختياري)

### تخصيص النطاق (Domain)

1. Settings → Domains
2. اضغط **"Add"**
3. أدخل نطاقك (مثل `chat.yourdomain.com`)
4. اتبع التعليمات لإضافة DNS records

### تفعيل HTTPS (تلقائي)

Vercel يوفر SSL/HTTPS تلقائياً لجميع النطاقات ✅

### مراقبة الأداء

1. Analytics → Web Analytics
2. اضغط **"Enable"**
3. شاهد إحصائيات الزوار في الوقت الفعلي

---

## 💰 التكلفة

### Vercel (المنصة)
- **المجاني:** مناسب للتطوير والاستخدام الشخصي
  - Bandwidth: 100GB/month
  - Deployments: غير محدودة
  - Serverless Functions: 100 ساعة/month

- **Pro ($20/month):** للإنتاج
  - Bandwidth: 1TB/month
  - أولوية البناء
  - دعم متقدم

### Vercel Postgres
- **المجاني:**
  - 256 MB storage
  - 60 ساعة compute/month
  - مناسب للبداية

- **Pro:** حسب الاستخدام

### AI APIs

| المزود | السعر التقريبي |
|--------|----------------|
| **OpenAI GPT-4** | $0.03 لكل 1K tokens |
| **OpenAI GPT-3.5** | $0.002 لكل 1K tokens |
| **DeepSeek** | $0.0014 لكل 1K tokens (أرخص!) |
| **Gemini** | مجاني حتى حد معين |

**نصيحة:** ابدأ بـ DeepSeek أو Gemini لتوفير التكلفة!

---

## 📊 بعد النشر

### 1. اختبار الدردشة

افتح: `https://your-app.vercel.app/chat`

جرّب:
```
اكتب لي كود Python بسيط
```

### 2. مراقبة الـ Logs

1. في لوحة تحكم Vercel
2. اختر المشروع
3. اذهب إلى **"Functions"**
4. شاهد logs الـ API calls

### 3. مشاركة الرابط

شارك التطبيق مع الآخرين:
```
https://your-app.vercel.app/chat
```

---

## 🎯 نصائح للإنتاج

### 1. استخدام Environment Variables محلية

لا تضع API keys مباشرة في الكود!

```typescript
// ✅ صحيح
const apiKey = process.env.OPENAI_API_KEY

// ❌ خطأ
const apiKey = 'sk-proj-...'
```

### 2. Rate Limiting

أضف حماية ضد الاستخدام المفرط:

```typescript
// server/middleware/rate-limit.ts
export default defineEventHandler(async (event) => {
  const ip = getRequestIP(event)
  // تحقق من عدد الطلبات من نفس IP
})
```

### 3. Monitoring

راقب استخدام AI:
- عدد الرسائل
- التكلفة
- الأخطاء

### 4. Backup

احفظ نسخة احتياطية من المحادثات:
```bash
# تصدير من قاعدة البيانات
pg_dump $POSTGRES_URL > backup.sql
```

---

## 🔄 التحديثات التلقائية

Vercel ينشر تلقائياً عند:
- Push إلى الفرع الرئيسي
- Merge Pull Request

لإيقاف النشر التلقائي:
1. Settings → Git
2. عطّل **"Auto Deploy"**

---

## 📞 الدعم

### مشاكل Vercel
- Docs: https://vercel.com/docs
- Support: https://vercel.com/support

### مشاكل التطبيق
- GitHub Issues: https://github.com/MOTEB1989/WejdanAI/issues
- التوثيق: [docs/CHAT_INTERFACE.md](docs/CHAT_INTERFACE.md)

---

## 🎉 تم بنجاح!

الآن لديك:
- ✅ تطبيق دردشة ذكي منشور
- ✅ رابط عام يمكن الوصول إليه من أي مكان
- ✅ HTTPS آمن
- ✅ قاعدة بيانات Postgres
- ✅ تكامل AI متعدد

**استمتع بالدردشة مع وجدان AI!** 🤖✨

---

**تم التوليد آلياً بواسطة نظام WejdanAI** 🚀
