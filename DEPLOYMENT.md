# 🚀 دليل النشر - WejdanAI Smart Bank

## النشر على Vercel (الطريقة الأسهل والمجانية)

### الخطوة 1: إعداد قاعدة بيانات Postgres

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر "Storage" من القائمة الجانبية
3. انقر على "Create Database"
4. اختر "Postgres"
5. أدخل اسم قاعدة البيانات (مثلاً: wejdanai-db)
6. اختر المنطقة الأقرب لك
7. انقر على "Create"

### الخطوة 2: الحصول على رابط الاتصال

1. بعد إنشاء قاعدة البيانات، اذهب إلى تبويب "Settings"
2. انسخ `POSTGRES_URL` من قسم "Connection String"
3. احتفظ بهذا الرابط، ستحتاجه لاحقاً

### الخطوة 3: نشر التطبيق

#### الطريقة الأولى: باستخدام زر Deploy

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/MOTEB1989/WejdanAI&env=POSTGRES_URL&envDescription=PostgreSQL%20connection%20string%20from%20Vercel%20Postgres&project-name=wejdanai-bank&repository-name=WejdanAI)

1. انقر على الزر أعلاه
2. سجل الدخول إلى Vercel (أو أنشئ حساب جديد)
3. اختر اسم المشروع
4. أضف متغير البيئة:
   - المفتاح: `POSTGRES_URL`
   - القيمة: الرابط الذي نسخته في الخطوة 2
5. انقر على "Deploy"
6. انتظر اكتمال النشر (2-3 دقائق)
7. 🎉 تطبيقك جاهز!

#### الطريقة الثانية: من Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel

# أضف متغير البيئة
vercel env add POSTGRES_URL

# إعادة النشر
vercel --prod
```

### الخطوة 4: التحقق من التطبيق

1. افتح الرابط الذي أعطاك إياه Vercel
2. اضغط على "بدء استخدام البنك"
3. جرب المساعد الذكي بكتابة "ما هو رصيدي؟"

---

## النشر على Netlify

```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# تسجيل الدخول
netlify login

# بناء المشروع
pnpm build

# النشر
netlify deploy --prod
```

بعد النشر، أضف متغير البيئة `POSTGRES_URL` في لوحة تحكم Netlify.

---

## النشر على Railway

1. اذهب إلى [Railway](https://railway.app)
2. انقر على "New Project"
3. اختر "Deploy from GitHub repo"
4. اختر مستودع WejdanAI
5. أضف متغير البيئة `POSTGRES_URL`
6. Railway سينشئ قاعدة بيانات Postgres تلقائياً
7. انتظر اكتمال النشر

---

## النشر على VPS (Ubuntu/Debian)

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# تثبيت PostgreSQL
sudo apt install postgresql postgresql-contrib

# إنشاء قاعدة بيانات
sudo -u postgres createdb wejdanai

# استنساخ المشروع
git clone https://github.com/MOTEB1989/WejdanAI.git
cd WejdanAI

# تثبيت pnpm
npm i -g pnpm

# تثبيت التبعيات
pnpm install

# إنشاء ملف .env
echo "POSTGRES_URL=postgresql://postgres:password@localhost:5432/wejdanai" > .env

# بناء المشروع
pnpm build

# تشغيل باستخدام PM2
npm i -g pm2
pm2 start npm --name "wejdanai" -- start
pm2 save
pm2 startup
```

---

## استكشاف الأخطاء

### خطأ: "relation does not exist"
- **الحل**: التطبيق سينشئ الجداول تلقائياً عند أول زيارة. افتح التطبيق وانتظر قليلاً.

### خطأ: "Could not connect to database"
- **الحل**: تأكد من صحة `POSTGRES_URL` في متغيرات البيئة.

### خطأ: "Build failed"
- **الحل**: تأكد من أن Node.js إصدار 18 أو أحدث.

### واجهة فارغة
- **الحل**: افتح أدوات المطور (F12) وتحقق من console للأخطاء.

---

## الأمان في الإنتاج

⚠️ **مهم جداً:**

1. **تغيير كلمة المرور الافتراضية**
   - كلمة المرور التجريبية: `demo123`
   - غيرها فوراً في الإنتاج

2. **استخدام HTTPS**
   - Vercel يوفر SSL مجاناً
   - لا تنشر بدون HTTPS

3. **حماية قاعدة البيانات**
   - استخدم كلمة مرور قوية
   - فعّل SSL للاتصال

4. **متغيرات البيئة**
   - لا تكشف `POSTGRES_URL` أبداً
   - لا تضعها في Git

---

## الحصول على رابط مخصص

### Vercel
```bash
# إضافة دومين مخصص
vercel domains add yourdomain.com
```

### Cloudflare (مجاني)
1. سجل دومين في Namecheap أو GoDaddy
2. أضفه إلى Cloudflare
3. وجّه DNS إلى Vercel

---

## المراقبة والتحليلات

### Vercel Analytics (مجاني)
- مدمج تلقائياً
- لا حاجة لإعدادات

### Google Analytics
أضف هذا الكود في `nuxt.config.ts`:

```typescript
export default defineNuxtConfig({
  app: {
    head: {
      script: [
        {
          src: 'https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX',
          async: true,
        },
      ],
    },
  },
})
```

---

## النسخ الاحتياطي

### النسخ الاحتياطي اليدوي

```bash
# تصدير قاعدة البيانات
pg_dump -U username -h hostname database > backup.sql

# استعادة قاعدة البيانات
psql -U username -h hostname database < backup.sql
```

### النسخ الاحتياطي التلقائي (Vercel)
- Vercel يحفظ نسخ احتياطية تلقائياً
- يمكن الاستعادة من Dashboard

---

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من [Issues في GitHub](https://github.com/MOTEB1989/WejdanAI/issues)
2. افتح Issue جديد إذا لزم الأمر
3. راسلنا على support@wejdanai.com

---

**نشر سعيد! 🚀**
